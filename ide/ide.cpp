#include <array>
#include <string>
#include <thread>

namespace ide {
    
    unsigned int compile(string codeLines) {
        
        for (i = new uint8_t<round(code.length()) + 1>; i < code.length(); i++) {
            
            
            
        };
        
    };
    
}
