#include <fstream>
#include <string>

namespace map {
    
    unsigned int generate(uint1_t random, string name, string seed) {
        
        if (random == 0) {
            
            string fileContent = ""
            
        } else {
            
            
            
        };
        
    };
    
}
